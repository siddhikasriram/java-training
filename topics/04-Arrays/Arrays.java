public class Arrays {
    public static void main(String[] args) {
        int[] arr1 = {1, 2, 3, 4, 5}; // Initialization of arrays
        int[] arr = new int[5]; // Declaring an array of integers
        for (int i = 0; i < arr.length; i++) {
            System.out.println("Element " + i + ": " + arr[i]); // Prints default value (0)
        }
    }
}
